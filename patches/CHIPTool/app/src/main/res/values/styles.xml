<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/theme_bg</item>
        <item name="colorSurface">@color/theme_surface</item>
        <item name="colorOnSurface">@color/theme_on</item>
        
        <!-- Standard-Card-Styling für Material 3 Look -->
        <item name="materialCardViewStyle">@style/CustomCardViewStyle</item>
    </style>

    <style name="CustomCardViewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/theme_surface</item>
        <item name="strokeColor">@color/theme_outline</item>
        <item name="strokeWidth">1dp</item>
        <item name="cardElevation">0dp</item>
    </style>

    <style name="title_text" parent="TextAppearance.AppCompat.Title" />
    <style name="info_text" parent="TextAppearance.AppCompat.Medium" />
    <style name="mini_info_text" parent="TextAppearance.AppCompat.Small" />
</resources>
